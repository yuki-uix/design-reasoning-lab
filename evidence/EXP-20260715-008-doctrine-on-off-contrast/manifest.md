# Evidence manifest: EXP-20260715-008-doctrine-on-off-contrast

Pre-run manifest, created at registration per CONTRIBUTING ("Before an experiment"). No evidence collected yet; fields that can only be known at run time are marked **not yet known**.

| File | Type | Captured at | Provenance | SHA-256 | Notes |
| --- | --- | --- | --- | --- | --- |
| *(none yet — expected files listed below)* | | | | | |

Expected per run, in `run-<N>-<on|off>/`:

- Full transcript (ON: sidebar chat log + Claude Code session file; OFF: terminal session transcript)
- Exported `.pen` document at run end
- `get_jsx` output of the root frame (text file)
- Undo-history panel screenshot
- Screen recording (may use Git LFS)
- `versions.md` — OpenPencil app version, Claude Code version, model ID, OS, timestamps

## Environment

- Product and visible version: OpenPencil desktop app, version **not yet known** (recorded at run; may differ from EXP-007's pinned source commit `12031b76` — both will be recorded)
- Harness: Claude Code, version **not yet known**; model ID **not yet known** (same in both conditions, recorded per run)
- Access date: **not yet known** (run date)
- Account or feature context: yuki's local install; MCP server on default port; terminal runs from an empty working directory (no CLAUDE.md, no `.claude/`)
- Browser/device: macOS desktop, version recorded at run

## Transformations and redactions

None yet. Measure extraction per the pre-registered scoring rules will be recorded here with its script.

## Missing evidence

All evidence pending: this is a pre-run manifest. The experiment has status `planned`; runs have not occurred.
